# Modeling VAR in Python


# First, let's import the libraries and functions we are going to need
In [3]:

import numpy as np
import pandas
import statsmodels.api as sm
from statsmodels.tsa.api import VAR, DynamicVAR
